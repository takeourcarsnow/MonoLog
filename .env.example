# MonoLog environment example
# Copy this file to .env.local and fill values for Supabase if you want remote storage.

# Choose backend mode: "local" (default) or "supabase"
NEXT_PUBLIC_MODE=local

# If you set NEXT_PUBLIC_MODE=supabase provide these values from your Supabase project.
# These are public/anon keys that the client uses. Keep service_role keys secret and only use them server-side.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
